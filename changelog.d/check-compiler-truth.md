`native check` on a TypeScript core now ends with the compiler-truth pass: the pinned external core compiler's analyzer runs over the entry with the shipped SDK declarations mapped, so the check verdict and the build verdict come from one compiler. Type errors the frontend's own line would miss fail the check with the compiler's diagnostics verbatim; an analyzer that cannot reach a verdict defers to the build instead of wedging check.
